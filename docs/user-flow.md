# 사용자 플로우 다이어그램
# mvp-builder

> 작성일: 2026-03-17
> 작성자: UX/Product Design Agent (4단계)
> 기반 문서: `docs/PRD.md`, `docs/MVP-scope.md`, `docs/user-persona.md`, `docs/api-spec.md`, `docs/wireframe.md`
> MVP In-scope 기능(F-01~F-08)만 다룬다.

---

## 1. 시나리오 목록

| 번호 | 제목 | 대상 페르소나 | 핵심 기능 |
|------|------|------------|----------|
| SC-01 | 회원가입 및 이메일 인증 | 전체 (신규 사용자) | F-06 |
| SC-02 | 비개발자의 첫 번째 MVP 생성 | 페르소나 1 — 박지수 | F-01, F-02, F-03, F-04, F-05 |
| SC-03 | 개발자의 기술 스택 지정 후 MVP 생성 | 페르소나 2 — 이태양 / 페르소나 3 — 김현우 | F-01, F-02, F-03, F-04, F-05, F-08 |
| SC-04 | 생성 이력에서 clone URL 재확인 | 페르소나 1 — 박지수 (시나리오 2) | F-07 |
| SC-05 | 생성 실패 및 재시도 | 전체 | F-02, F-03 |

---

## 2. 공통 플로우

### CF-01 로그인 상태 확인 및 인증 필요 처리

인증이 필요한 모든 화면에 진입 전 공통으로 수행되는 흐름이다.

```mermaid
flowchart TD
    A([페이지 접근]) --> B{메모리(Zustand store)에\nAccess Token 존재?}
    B -->|없음| C[로그인 페이지로 리다이렉트]
    B -->|있음| D{Access Token\n유효성 확인}
    D -->|유효| E([정상 진입])
    D -->|만료 401| F[POST /auth/refresh 호출]
    F --> G{Refresh Token\n유효?}
    G -->|유효| H[새 Access Token 발급\n원래 요청 재시도]
    H --> E
    G -->|만료/무효 401| I[Zustand store Access Token 초기화]
    I --> C
```

---

### CF-02 공통 에러 처리

네트워크 오류, 서버 에러(5xx) 등 전역에서 처리되는 흐름이다.

```mermaid
flowchart TD
    A([API 호출]) --> B{응답}
    B -->|2xx 성공| C([정상 처리])
    B -->|4xx 클라이언트 에러| D[에러 코드별 인라인 메시지 표시]
    D --> E([사용자에게 피드백])
    B -->|5xx 서버 에러| F[토스트 알림: 서버 오류가 발생했습니다.\n잠시 후 다시 시도해주세요.]
    F --> G([사용자에게 피드백])
    B -->|네트워크 에러| H[토스트 알림: 네트워크 연결을\n확인해주세요.]
    H --> G
```

---

## 3. 시나리오별 상세 플로우

### SC-01 회원가입 및 이메일 인증

**대상 페르소나**: 전체 신규 사용자
**트리거**: 랜딩 페이지에서 "시작하기" 또는 "무료로 시작하기" 클릭
**종료점**: 로그인 완료 후 메인 생성 페이지 진입

```mermaid
flowchart TD
    START([랜딩 페이지 방문]) --> A[시작하기 클릭]
    A --> B[S-02 회원가입 페이지 이동]
    B --> C[이메일 / username / 비밀번호 입력]
    C --> D{클라이언트 측 유효성 검사}
    D -->|실패| E[인라인 에러 표시\n예: 비밀번호 조건 미충족]
    E --> C
    D -->|통과| F[POST /auth/register 호출]
    F --> G{API 응답}
    G -->|201 Created| H[S-03 이메일 인증 안내 페이지 이동]
    G -->|409 USER_001\n이메일 중복| I[인라인 에러: 이미 사용 중인 이메일입니다.]
    I --> C
    G -->|409 USER_002\nusername 중복| J[인라인 에러: 이미 사용 중인 사용자 이름입니다.]
    J --> C
    G -->|400 유효성 실패| K[서버 유효성 에러 인라인 표시]
    K --> C

    H --> L{사용자 메일함 확인}
    L -->|인증 링크 클릭| M[GET /auth/verify-email?token=... 호출]
    L -->|메일 못 받음| N[재발송 버튼 클릭]
    N --> O[POST /auth/resend-verification 호출]
    O --> P[재발송 안내 인라인 표시]
    P --> L

    M --> Q{토큰 유효성}
    Q -->|유효 200| R[S-04 인증 완료 페이지\n성공 메시지 표시]
    Q -->|만료/무효 400\nEMAIL_001| S[S-04 인증 실패 페이지\n에러 메시지 표시]
    S --> T[인증 메일 재발송하기 클릭]
    T --> N

    R --> U[로그인하기 클릭]
    U --> V[S-05 로그인 페이지 이동]
    V --> END([SC-01 종료 → SC-02/SC-03 진입])
```

---

### SC-02 비개발자의 첫 번째 MVP 생성

**대상 페르소나**: 페르소나 1 — 박지수 (기술 친숙도: 하)
**트리거**: 로그인 후 메인 페이지 진입
**종료점**: clone URL 수령 및 공유

```mermaid
flowchart TD
    START([S-06 메인 생성 페이지]) --> A[요구사항 텍스트에어리어에 아이디어 입력]
    A --> B{글자 수 확인}
    B -->|10,000자 초과| C[글자 수 경고 표시\n초과분 입력 차단]
    C --> A
    B -->|범위 이내| D[개발자 옵션 패널 — 닫힘 유지\n기본값 사용]
    D --> E[MVP 생성 시작 버튼 클릭]
    E --> F{요구사항 비어있음?}
    F -->|비어있음| G[인라인 에러: 요구사항을 입력해주세요.]
    G --> A
    F -->|입력됨| H[POST /generation 호출]
    H --> I{API 응답}
    I -->|201 jobId 반환| J[S-07 생성 진행 화면으로 상태 전환\nGET /generation/:jobId/stream SSE 연결]
    I -->|409 GEN_001\n이미 진행 중인 작업| K[모달: 이미 진행 중인 생성 작업이 있습니다.\n진행 상황을 보시겠습니까?]
    K -->|확인| L[기존 jobId로 S-07 진행 화면 이동]
    K -->|취소| A

    J --> M[SSE 이벤트 수신 시작]
    M --> N{이벤트 타입}
    N -->|connected| O[연결 확인 표시]
    O --> M
    N -->|progress| P[단계명 + 진행률 바 업데이트\nStageChecklist 상태 업데이트]
    P --> Q{사용자 이탈 시도?}
    Q -->|이탈 시도| R[이탈 경고 모달 표시]
    R -->|계속 생성하기| P
    R -->|나가기| S[SSE 연결 종료\nS-06 초기화]
    Q -->|이탈 없음| M
    N -->|completed| T[S-08 생성 완료 화면으로 상태 전환]
    N -->|error| U[에러 상태 화면 표시\n에러 메시지 출력]
    N -->|timeout| V[타임아웃 상태 화면 표시]
    U --> W{사용자 선택}
    W -->|다시 시도하기| A
    W -->|처음으로| A
    V --> X[요구사항 수정 후 다시 시도하기]
    X --> A

    T --> Y[clone URL 표시\n복사 버튼 제공]
    Y --> Z{사용자 행동}
    Z -->|URL 복사 클릭| AA[클립보드 복사 완료 토스트]
    AA --> Z
    Z -->|GitHub에서 열기 클릭| AB[새 탭으로 GitHub 저장소 열기]
    AB --> Z
    Z -->|새 MVP 만들기 클릭| AC[S-06 초기화]
    Z -->|이력 링크 클릭| AD[S-09 생성 이력 페이지 이동]
    END([SC-02 종료])
```

---

### SC-03 개발자의 기술 스택 지정 후 MVP 생성

**대상 페르소나**: 페르소나 2 — 이태양 / 페르소나 3 — 김현우 (기술 친숙도: 상)
**트리거**: 로그인 후 메인 페이지 진입
**종료점**: clone URL로 저장소 clone 및 실행 확인

```mermaid
flowchart TD
    START([S-06 메인 생성 페이지]) --> A[요구사항 텍스트에어리어에 상세 요구사항 입력]
    A --> B[개발자 옵션 패널 클릭하여 펼침]
    B --> C{옵션 입력 방식}
    C -->|선택지에서 고름| D[ComboBox에서 선택\n예: Node.js + NestJS + React]
    C -->|직접 입력| E[ComboBox 자유 입력\n예: Django + Vue.js]
    D --> F[기술 스택 확정]
    E --> F
    F --> G{아키텍처 선택}
    G -->|선택| H[아키텍처 확정\n예: monolith]
    H --> I{배포 방식 선택}
    I -->|선택| J[배포 방식 확정\n예: AWS EC2 + RDS]
    J --> K[MVP 생성 시작 버튼 클릭]
    K --> L[POST /generation 호출\ndeveloperOptions 포함]
    L --> M{API 응답}
    M -->|201 jobId 반환| N[S-07 생성 진행 화면 전환\nSSE 연결 시작]
    M -->|409 GEN_001| O[진행 중 작업 확인 모달]
    O -->|확인| P[기존 jobId 진행 화면 이동]
    O -->|취소| A

    N --> Q[SSE progress 이벤트 수신]
    Q --> R[단계별 진행률 실시간 업데이트\nanalyzing → documenting → developing → testing → uploading]
    R --> S{SSE completed 이벤트}
    S -->|completed| T[S-08 완료 화면 전환\nclone URL, repoName 표시]
    S -->|계속 대기| R

    T --> U[clone URL 복사]
    U --> V[터미널에서 git clone 실행]
    V --> W[npm install && npm run dev 실행]
    W --> X{정상 실행 확인}
    X -->|정상 실행| Y[비즈니스 로직 직접 추가 시작]
    X -->|실행 오류| Z[GitHub 저장소 이슈 확인\n또는 새 MVP 생성 요청]
    END([SC-03 종료])
```

> 가정: SC-03의 V~Z 단계(로컬 실행)는 서비스 외부 동작이다. UX 흐름상 완료 화면에서 시작 가이드를 제공하는 것으로 서비스 책임이 종료된다.

> [이슈 9-1] 페르소나 3 — 김현우의 핵심 시나리오 중 "파일 구조와 모듈 분리 방식을 확인"하는 시나리오는 현재 MVP에서 별도 플로우로 지원하지 않는다. 파일 트리 미리보기(F-10)는 Out-of-scope(v1.1 구현 예정)이므로, 현재 MVP에서는 clone 후 로컬에서 직접 확인하는 방식으로 안내한다. 완료 화면(S-08)의 시작 가이드가 이 안내를 대체한다.

---

### SC-04 생성 이력에서 clone URL 재확인

**대상 페르소나**: 페르소나 1 — 박지수 (시나리오 2: 미팅 1주일 후 재접속)
**트리거**: 재로그인 후 이력 페이지 진입
**종료점**: clone URL 복사 완료

```mermaid
flowchart TD
    START([재접속 후 S-05 로그인]) --> A[이메일/비밀번호 입력 후 로그인]
    A --> B{로그인 응답}
    B -->|200 성공| C[S-06 메인 생성 페이지 진입]
    B -->|401 AUTH_001| D[인라인 에러: 이메일 또는 비밀번호가 올바르지 않습니다.]
    D --> A

    C --> E[네비게이션에서 이력 클릭]
    E --> F[GET /generation 호출\npage=1 limit=20]
    F --> G{API 응답}
    G -->|200 목록 반환| H[S-09 생성 이력 목록 표시]
    G -->|데이터 없음\ntotal=0| I[빈 상태 화면\n첫 번째 MVP 만들기 CTA]

    H --> J{원하는 이력 탐색}
    J -->|상태 필터 적용| K[status=completed 필터 선택]
    K --> L[GET /generation?status=completed 재호출]
    L --> H
    J -->|페이지 이동| M[다음 페이지 클릭]
    M --> N[GET /generation?page=2 호출]
    N --> H
    J -->|이력 카드 복사 클릭| O[clone URL 클립보드 복사]
    O --> P[복사 완료 토스트 표시]
    P --> END([SC-04 종료])

    J -->|상세 보기 클릭| Q[GET /generation/:jobId 호출]
    Q --> R{API 응답}
    R -->|200 상세 반환| S[S-10 생성 상세 페이지 표시]
    R -->|404 GEN_002| T[토스트: 생성 작업을 찾을 수 없습니다.]
    T --> H
    S --> U[clone URL 복사 클릭]
    U --> P
    S --> V[뒤로가기 클릭]
    V --> H
```

---

### SC-05 생성 실패 및 재시도

**대상 페르소나**: 전체
**트리거**: SC-02 또는 SC-03 진행 중 에러/타임아웃 이벤트 수신
**종료점**: 재시도 후 성공 또는 사용자 포기

```mermaid
flowchart TD
    START([S-07 생성 진행 중]) --> A{SSE 이벤트 수신}
    A -->|error 이벤트| B[에러 상태 화면 전환\n에러 메시지 표시\n예: 코드 생성 중 오류가 발생했습니다.]
    A -->|timeout 이벤트| C[타임아웃 상태 화면 전환\n요구사항 간소화 안내 메시지]
    A -->|네트워크 끊김\nSSE 연결 종료| D[연결 재시도 안내 표시\n자동 재연결 시도]
    D --> E{재연결 성공?}
    E -->|성공| F[GET /generation/:jobId 상태 조회]
    F --> G{현재 상태}
    G -->|processing| H[진행 화면 복원\n현재 진행률 표시]
    H --> A
    G -->|completed| I[완료 화면 표시\nclone URL 제공]
    G -->|failed| B
    E -->|실패 3회 이상| J[네트워크 오류 안내\n다시 시도하기 버튼]
    J --> K{사용자 선택}
    K -->|다시 시도| D
    K -->|나중에 이력에서 확인| L[S-09 생성 이력 페이지 이동]

    B --> M{사용자 선택}
    M -->|다시 시도하기| N[S-06 입력 화면 복원\n기존 요구사항과 옵션 유지]
    M -->|처음으로| O[S-06 초기화]
    N --> P[요구사항 수정 또는 그대로 재시도]
    P --> Q[POST /generation 재호출]
    Q --> R{API 응답}
    R -->|201 성공| S[새 jobId로 S-07 진행 화면]
    S --> A
    R -->|409 GEN_001\n이전 작업 처리 중| T[이전 작업이 아직 처리 중입니다.\n잠시 후 이력에서 확인하세요.]

    C --> U{사용자 선택}
    U -->|요구사항 수정 후 다시 시도| V[S-06 요구사항 입력 복원]
    V --> W[요구사항 단순화 후 재입력]
    W --> Q
    U -->|이력에서 확인| L

    END([SC-05 종료])
```

---

## 4. 플로우 간 관계 요약

```mermaid
flowchart LR
    CF01[CF-01\n로그인 상태 확인] -.->|모든 인증 필요 화면 진입 시| SC02
    CF01 -.->|모든 인증 필요 화면 진입 시| SC03
    CF01 -.->|모든 인증 필요 화면 진입 시| SC04
    CF02[CF-02\n공통 에러 처리] -.->|API 호출 실패 시 전역 적용| SC01
    CF02 -.->|API 호출 실패 시 전역 적용| SC02
    CF02 -.->|API 호출 실패 시 전역 적용| SC03
    CF02 -.->|API 호출 실패 시 전역 적용| SC04

    SC01[SC-01\n회원가입/인증] -->|완료 후 로그인| SC02
    SC01 -->|완료 후 로그인| SC03
    SC02[SC-02\n비개발자 생성] -->|생성 실패 시| SC05
    SC03[SC-03\n개발자 생성] -->|생성 실패 시| SC05
    SC02 -->|완료 후| SC04
    SC03 -->|완료 후| SC04
    SC04[SC-04\n이력 재확인]
    SC05[SC-05\n실패 및 재시도] -->|재시도| SC02
    SC05 -->|재시도| SC03
```

---

## 5. 엣지 케이스 정리

| 케이스 | 발생 시점 | 처리 방식 |
|--------|----------|----------|
| 회원가입 도중 동일 이메일 재가입 시도 | SC-01 / `POST /auth/register` | `409 USER_001` → 인라인 에러: "이미 사용 중인 이메일입니다." |
| 만료된 이메일 인증 링크 클릭 | SC-01 / `GET /auth/verify-email` | `400 EMAIL_001` → S-04 실패 화면 → 재발송 유도 |
| 이메일 미인증 상태로 로그인 시도 | SC-01, SC-02 / `POST /auth/login` | `403 AUTH_002` → 인라인 에러 + "인증 메일 재발송" 링크 제공 |
| 생성 진행 중 브라우저 탭 닫기/새로고침 | SC-02, SC-03 / S-07 | `beforeunload` 이벤트로 경고 모달 표시 (C-UX-12) |
| 생성 진행 중 네트워크 끊김 (SSE 연결 해제) | SC-02, SC-03, SC-05 | 자동 재연결 시도 → 실패 시 수동 재시도 안내 |
| 이미 진행 중인 생성 작업 있을 때 새 생성 요청 | SC-02, SC-03 / `POST /generation` | `409 GEN_001` → 기존 작업 확인 모달 표시 |
| Access Token 만료 중 SSE 연결 시도 | SC-02, SC-03 / `GET /generation/:jobId/stream` | Query Parameter `?token=<accessToken>` 만료 시 CF-01 Refresh 흐름 후 재연결 |
| 생성 완료 후 바로 이력 페이지 진입 시 | SC-04 / `GET /generation` | 최신 완료 항목이 목록 상단에 표시됨 |
| 이력 페이지에서 진행 중인 항목 클릭 | SC-04 / S-09 | S-07 생성 진행 화면으로 이동하여 현재 진행률 표시 |
| 10,000자 초과 요구사항 입력 | SC-02, SC-03 | 클라이언트: 글자 수 초과 시 입력 차단 + 경고 표시 / 서버: `400 GEN_003` |
| 생성 타임아웃 | SC-02, SC-03, SC-05 / SSE `timeout` | 타임아웃 화면 전환 + "요구사항을 간소화해주세요" 안내 |
| 빈 생성 이력 목록 | SC-04 / `GET /generation` | 빈 상태(empty state) 화면 + "첫 번째 MVP 만들기" CTA |
| 다른 사용자의 jobId로 직접 URL 접근 | SC-04 / `GET /generation/:jobId` | `403 Forbidden` → 이력 페이지로 리다이렉트 + 에러 토스트 |
