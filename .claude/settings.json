{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read f || exit 0; case \"$f\" in *apps/backend/src/*.ts) cd /Users/dadigom/WebStorm/mvp-builder/apps/backend && npx tsc --noEmit 2>&1 | tail -30;; esac; }"
          }
        ]
      }
    ]
  }
}
